# VM port. Use this same port in your Cloudflare origin / tunnel service URL.
APP_PORT=3000
# Default allows testing from another device on your LAN.
# Set 127.0.0.1 if cloudflared runs directly on this VM and LAN access is unneeded.
BIND_ADDRESS=0.0.0.0
